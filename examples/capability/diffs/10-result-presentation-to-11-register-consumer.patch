diff --git a/examples/capability/checkpoints/10-result-presentation/src/consumer.ts b/examples/capability/checkpoints/11-register-consumer/src/consumer.ts
--- a/examples/capability/checkpoints/10-result-presentation/src/consumer.ts
+++ b/examples/capability/checkpoints/11-register-consumer/src/consumer.ts
@@ -0,0 +1,2 @@
+import type { Context } from '@deepseek-ai/cordis'
+import { defineTool } from '@deepseek-ai/dsh-tools'
@@ -48,0 +51,17 @@
+
+export function apply(ctx: Context): void {
+  ctx.tools.register(defineTool({
+    name: 'summarize_text',
+    description: 'Summarize text with the configured summarizer.',
+    parameters: summaryParameters,
+    output: {
+      schema: summaryOutputSchema,
+      render: (_args, value) => renderSummary(value),
+    },
+    execute(args, exec) {
+      const spec = ctx.summarizer.resolve(args)
+      return ctx.summarizer.summarize(spec, exec.signal)
+    },
+    presentCall: presentSummaryCall,
+  }))
+}
