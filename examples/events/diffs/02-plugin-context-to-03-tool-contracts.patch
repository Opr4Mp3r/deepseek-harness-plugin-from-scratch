diff --git a/examples/events/checkpoints/03-tool-contracts/src/contracts.ts b/examples/events/checkpoints/03-tool-contracts/src/contracts.ts
new file mode 100644
--- /dev/null
+++ b/examples/events/checkpoints/03-tool-contracts/src/contracts.ts
@@ -0,0 +1,17 @@
+import type { ParameterSchemaSpec, ValueSchemaSpec } from '@deepseek-ai/dsh-tools'
+
+export const projectRuleParameters = {
+  rule: {
+    type: 'string',
+    required: true,
+    description: 'One project rule to preserve in model context.',
+  },
+} as const satisfies ParameterSchemaSpec
+
+export const projectRuleOutput = {
+  type: 'object',
+  additionalProperties: false,
+  properties: {
+    rule: { type: 'string', required: true },
+  },
+} as const satisfies ValueSchemaSpec
